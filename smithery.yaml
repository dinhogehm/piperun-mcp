# Configuração do Smithery para o PipeRun MCP
startCommand:
  type: stdio
  configSchema:
    type: object
    properties: {}
  commandFunction: |
    function(config) {
      return {
        command: 'node',
        args: ['index.js'],
        env: { PORT: '3000' }
      };
    }
